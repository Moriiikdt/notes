## 刷题笔记

### 二叉树DFS

递归找 递归返回值的条件设计

再写不同情况的 返回情况 二叉树翻转

```python
def dfs(node):
    if not node:
        return 基础值

    left = dfs(node.left)
    right = dfs(node.right)

    return 根据 left 和 right 计算结果
```

DFS模板

```
def dfs(状态参数):
    # 1. 终止条件（base case）
    if 满足结束条件:
        处理结果
        return

    # 2. 遍历所有可选项
    for choice in 可选择集合:
        # 2.1 做选择
        更新状态

        # 2.2 递归
        dfs(新状态)

        # 2.3 撤销选择（回溯）
        恢复状态

```

### BFS

```python
from collections import deque

queue = deque([起点])
step = 0

while queue:
    for _ in range(len(queue)):  # 当前层
        node = queue.popleft()
        if node 是目标:
            return step
        把 node 的邻居加入 queue
    step += 1

```

### 反转链表

```python
    def reverse(self,head):
        pre = None
        cur = head
        while cur:
            next_node = cur.next
            cur.next = pre
            pre = cur
            cur = next_node
        return pre
```

### 快慢指针 中点

```python
    def mid(self,head):
        fast = head
        slow = head
        while fast.next and fast.next.next:
            slow = slow.next
            fast = fast.next.next
        return slow 
```

[合并两个有序链表](https://leetcode.cn/problems/merge-two-sorted-lists/)

```python
class Solution(object):
    def mergeTwoLists(self, list1, list2):
        # 哨兵节点
        cur = out = ListNode()
        while list1 and list2:
            if list1.val < list2.val:
                cur.next = list1
                list1 = list1.next
            else:
                cur.next = list2
                list2 = list2.next
            cur = cur.next
        cur.next = list1 or list2
        return out.next
```

[排序链表](https://leetcode.cn/problems/sort-list/)

```python
# 快慢指针 找中点
# 两个有序链表（双指针）
# 递归 先分中点 再合并链表
class Solution(object):
    def sortList(self, head):
        if head == None or head.next == None: return head
        head2 = self.mid_find(head)

        head = self.sortList(head)
        head2 = self.sortList(head2)
        return self.merge_list(head, head2)
    
    # 找中点, 并断开链表
    def mid_find(self, head):
        fast = slow = head  
        while fast and fast.next:
            pre = slow
            slow = slow.next
            fast = fast.next.next
        pre.next = None
        return slow

    # 链表合并
    def merge_list(self, list1, list2):
        cur = tmp = ListNode()
        while list1 and list2:
            if list1.val < list2.val:
                cur.next = list1
                list1 = list1.next
            else:
                cur.next = list2
                list2 = list2.next
            cur = cur.next
        cur.next = list1 or list2
        return tmp.next

```

#### 环形链表II Floyd 判圈算法

```python
# 判圈并返回入口
class Solution(object):
    def detectCycle(self, head):
        slow = fast = head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            if fast is slow:
                while slow is not head:
                    head = head.next
                    slow = slow.next
                return slow
        return 
```

```python
# 判圈
class Solution(object):
    def hasCycle(self, head):
        slow = fast = head
        while fast and fast.next:
            slow = slow.next
            fast = fast.next.next
            if fast is slow:
                return True
        return False
```



### 单调栈 每日温度

```python
class Solution(object):
    def dailyTemperatures(self, temperatures):
        length = len(temperatures)
        stack = []
        ans = [0] * length

        for i in range(length):
            temperature = temperatures[i]
            # 将大于栈顶的数排出
            while stack and temperature > temperatures[stack[-1]]:
                out_id = stack.pop()
                ans[out_id] = i - out_id
            stack.append(i)
        return ans
```

### 排序问题

- 快排 纯递归解法

  ```python
  def quick_sort(nums):
      if len(nums) <= 1:
          return nums
      pivot = nums[0]
      left = [x for x in nums[1:] if x <= pivot]
      right = [x for x in nums[1:] if x > pivot]
      return quick_sort(left) + [pivot] + quick_sort(right)
  ```

- 正常写

  - Partition函数

  ```python
      def partition(self, nums, left, right):
          pivot = nums[left]
          i, j = left, right
          while i<j:
              # 从右往左
              while i<j and nums[j] >= pivot:
                  j-=1
              nums[i] = nums[j]
              # 从左往右
              while i<j and nums[i] < pivot:
                  i+=1
              nums[j] = nums[i]
          nums[i] = pivot
          return i
  ```

  - 递归

  ```python
      def quick_sort(self, nums, left, right):
          if left < right:
              index = self.partition(nums, left, right)
              self.quick_sort(nums, inde  x+1, right)
              self.quick_sort(nums, left, index-1)
  ```

  - TopK 问题

  ```python
      def findKthLargest(self, nums, k):
          self.quick_sort(nums, 0, len(nums)-1)
          return nums[len(nums)-k]
  ```

  

- 堆排序  大根堆

```python
class Solution(object):
    def findKthLargest(self, nums, k):
        n = len(nums)
        self.build_maxHeap(nums)
        for i in range(k-1):
            nums[0], nums[n-1-i] = nums[n-1-i], nums[0]
            self.down(nums, 0, n-1-i-1)
        return nums[0]
    
    def build_maxHeap(self, nums):
        n =len(nums)
        for root in range(n//2 , -1 , -1):
            self.down(nums, root, n-1)

    def down(self, nums, root, hi):
        # 存一手
        t = nums[root]
        # 左孩子
        child = 2 * root + 1 
        while child <= hi:
            # child拿大的那个
            if child +1 <= hi and nums[child]< nums[child+1]:
                child +=1
            # 本来root大就不换了
            if nums[child] < t:
                break
            # 换 
            nums[root] = nums[child]
            root = child
            child = root *2 +1
        nums[root] = t
```



- 冒泡排序 O(n²)

  ```python
  def bubble_sort(nums):
  	n = len(nums)
      for i in range(n):
          for j in range(n-i-1):
              if nums[j] > nums[j-1]:
                  nums[j-1], nums[j] = nums[j], nums[j-1]
  	return nums
  ```

- 拓扑排序

```python
class Solution(object):
    def canFinish(self, numCourses, prerequisites):
       # 首先第一步构建graph，python里面是要dict字典来构造
       graph = defaultdict(list)
       indegree = [0] * numCourses # 入度一开始大家都是0

       # 然后正式把prerequisite里面的关系写到我们的graph里面
       for dest, src in prerequisites: # 这里dest是destination，src是source缩写，方便理解是src->dest
           graph[src].append(dest) # [src:[dest1,dest2,dest3,...]]
           indegree[dest] += 1 # 更新需要src作为先修课的课程，给他们入度加1
       
       # 所以我们先构造一个queue来存储这些indegree==0
       queue = deque([i for i in range(numCourses) if indegree[i] == 0])

       # 再下一步我们开始学习这些indegree为0的课程：
       learned_course = 0
       while queue:
           course = queue.popleft() # 我们要用队列FIFO，从而实现BFS，所以popleft
           learned_course += 1
           
           # 对于是当前要学习的course是自己先修课的那些next_course，我们找出来
           for next_course in graph[course]:
               indegree[next_course] -= 1 # 给他们入度-1，意味着他们先修课要求-1
               if indegree[next_course] == 0: # 如果这些next course已经没有先修课了
                   queue.append(next_course) # 说明下一次循环他们就可以被学习了，加入队列

       return learned_course == numCourses
```

#### 岛屿问题

200.岛屿数量

```python
class Solution(object):
    def numIslands(self, grid):
        count = 0
        if not grid: return 0

        # 递归找陆地，越界或者不是陆地就直接返回
        def dfs(i,j):
            if i<0 or i>=len(grid) or j<0 or j>=len(grid[0]) or grid[i][j] != "1":
                return
            # 标记已遍历过
            grid[i][j] = "2"
            dfs(i-1,j)
            dfs(i+1,j)
            dfs(i,j-1)
            dfs(i,j+1)

        # 找到路径就四个方向找，直到没有陆地，最后加1
        for i in range(len(grid)):
            for j in range(len(grid[0])):
                if grid[i][j] == "1":
                    dfs(i,j)
                    count+=1
        return count
            
```



### 动态规划问题

思路： 选或者不选 / 选哪个 从最后或开始开始想

- 定义子问题
- 写出子问题的递推关系
- 确定 DP 数组的计算顺序 **自底向上的 dp 数组** / 备忘录的递归方法
- 空间优化（可选）

**小偷问题**

```python
class Solution(object):
    def rob(self, nums):
        if len(nums) == 0:
            return 0
        
        # 子问题是如何偷K间房子 也就是n[k-1] 
        # 不偷这个房子，那么问题就变成在前 k−1 个房子中偷到最大的金额 
        # 如果偷 那就是前 k−2 个房子中偷到的最大的金额 加上 这件房子的金额
        N = len(nums)
        dp = [0] * (N+1)
        dp[0] = 0
        dp[1] = nums[0]
        for k in range(2, N+1):
            dp[k] = max(dp[k-1], nums[k-1]+dp[k-2])
        
        return dp[N]
        
```

**最大全1 矩阵**

```python
class Solution(object):
    def maximalSquare(self, matrix):
        if len(matrix)==0 or len(matrix[0])==0:
            return 0
        
        max_side = 0
        rows, columns = len(matrix), len(matrix[0])
        dp = [[0]*columns for _ in range(rows)]

        for i in range(rows):
            for j in range(columns):
                if matrix[i][j] == "1":
                    if i == 0 or j == 0:
                        dp[i][j] = 1
                    else:
                        dp[i][j] = min(dp[i-1][j],dp[i][j-1],dp[i-1][j-1]) + 1
                    max_side = max(max_side, dp[i][j])
        
        return max_side* max_side
```

[乘积最大子数组](https://leetcode.cn/problems/maximum-product-subarray/)/ 和最大子数组

```python
class Solution(object):
    # 乘就考虑三种情况 正的乘正的 负的乘负的 只用自己 全部用max和min来维护 加就只考虑+就行 
    def maxProduct(self, nums):
        n = len(nums)
        f_min = [0] * n
        f_max = [0] * n
        f_max[0] = f_min[0] = nums[0]
        for i in range(1, n):
            x = nums[i]
            f_max[i] = max(f_max[i-1]*x, f_min[i-1]*x, x)
            f_min[i] = min(f_max[i-1]*x, f_min[i-1]*x, x)
        return max(f_max)
        
```

### LRU实现

```python
class Node:
    __slots__ = 'prev', 'next', 'key', 'value'

    def __init__(self, key=0, value=0):
        self.key = key
        self.value = value

class LRUCache:
    def __init__(self, capacity):
        self.capacity = capacity
        self.dummy = Node()
        self.dummy.pre = self.dummy
        self.dummy.next = self.dummy
        self.key2node = {}

    # 获取节点，把节点移到链表头部
    def get_node(self, key):
        if key not in self.key2node:
            return None
        node = self.key2node[key]
        # 先抽
        self.remove(node)
        # 再推到头部
        self.push_front(node)
        return node
    
    def get(self, key):
        node = self.get_node(key)
        return node.value if node else -1

    def put(self, key, value):
        # 如果有就先移
        node = self.get_node(key)
        if node:
            node.value = value
            return
        # 没有就新建
        self.key2node[key] = node = Node(key, value)
        self.push_front(node)
        # 超额 就删除最后一个
        if len(self.key2node) > self.capacity:
            back = self.dummy.prev
            del self.key2node[back.key]
            self.remove(back)

    # 删除
    def remove(self, x):
        # 先断前
        x.prev.next = x.next
        # 断后
        x.next.prev = x.prev

    # 推到最前
    def push_front(self, x):
        # 先管自己
        x.prev = self.dummy
        x.next = self.dummy.next
        # 再管插入后的前后
        x.prev.next = x
        x.next.prev = x

```

单词拆分

```python
class Solution(object):
    def wordBreak(self, s, wordDict):
        max_len = max(map(len, wordDict))
        words = set(wordDict)

        @cache
        def dfs(i):
            if i == 0:
                return True
            for j in range(i-1, max(i-max_len-1, -1), -1):
                if s[j:i] in words and dfs(j):
                    return True
            return False
        return dfs(len(s))
```

```python
class Solution(object):
    def wordBreak(self, s, wordDict):
        max_len = max(map(len, wordDict))
        words = set(wordDict)
        
        n = len(s)
        f = [True] + [False] * n
        for i in range(1, n+1):
            for j in range(i-1, max(i-max_len-1, -1), -1):
                if f[j] and s[j:i] in words:
                    f[i] = True
                    break
        return f[n]
```

回文子串

```python
class Solution:
    def countSubstrings(self, s: str) -> int:
        dp = [[False] * len(s) for _ in range(len(s))]
        result = 0
        for i in range(len(s) - 1, -1, -1):
            for j in range(i, len(s)):
                if s[i] == s[j]:
                    if j - i <= 1:
                        result += 1
                        dp[i][j] = True
                    elif dp[i + 1][j - 1]:
                        result += 1
                        dp[i][j] = True
        return result
```

